name: Shivansh Master Playlist
on:
  schedule:
    - cron: "*/15 * * * *"   # Runs every 15 minutes
  workflow_dispatch:

concurrency:
  group: shivansh-m3u
  cancel-in-progress: false   # ✅ Queue new runs instead of canceling

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Normalize sources.txt
        run: dos2unix sources.txt || true

      - name: Combine M3U files
        id: combine
        shell: bash
        run: |
          TIME_IST=$(TZ="Asia/Kolkata" date +"%Y-%m-%d %H:%M:%S IST")
          TMP_FILE="Shivansh_temp.m3u"
          > "$TMP_FILE"

          echo "Merging playlists from $(wc -l < sources.txt) sources..."
          cat sources.txt

          # Fetch and merge all URLs
          while IFS= read -r url; do
            url="${url%%#*}"
            url="$(echo -n "$url" | tr -d '\r')"
            if [ -n "$url" ]; then
              echo "Fetching: $url"
              curl -sL --max-time 30 --retry 3 --retry-delay 5 "$url" >> "$TMP_FILE"
              echo "" >> "$TMP_FILE"
            fi
          done < sources.txt

          # ✅ Replace old ygxworld credits with Shivansh credits
          sed -i 's|#EXTINF:-1 tvg-id="12321" group-title="Movies" tvg-logo="https://t.me/ygxworld",by @ygxworld|#EXTINF:-1 tvg-id="1" group-title="Credits" tvg-logo="https://i.postimg.cc/MZftPYPB/IMG-20250710-192112-387.jpg",@Shivansh|g' "$TMP_FILE"
          echo "✅ Credits updated successfully for $P Exclusive (Shivansh)"

          # ✅ Replace all general branding too
          sed -i 's|tvg-logo="https://t.me/ygxworld",by @ygxworld|tvg-logo="https://i.postimg.cc/MZftPYPB/IMG-20250710-192112-387.jpg", @Shivansh|g' "$TMP_FILE"

          # Count total and updated channels
          TOTAL_CHANNELS=$(grep -c '^#EXTINF' "$TMP_FILE" || true)
          UPDATED_CHANNELS=$TOTAL_CHANNELS

          # ✅ Header for $P Exclusive (Shivansh)
          {
            echo '# Pushed and Updated by Shivansh'
            echo '# Coded & Scripted by Kittujk'
            echo "# Last updated: $TIME_IST"
            echo "# Channels : Total - $TOTAL_CHANNELS | Updated - $UPDATED_CHANNELS"
            echo "" # 👈 Blank line after header
            echo '#EXTM3U billed-msg="Join Telegram @ ShivanshPlaylists"'
            echo ""  # 👈 Blank line after header
          } > Shivansh.m3u

          # Append playlist content and footer
          cat "$TMP_FILE" >> Shivansh.m3u
          echo "# ---- Created Exclusively for $P Exclusive (Shivansh) ----" >> Shivansh.m3u
          echo "" >> Shivansh.m3u

          echo "✅ Total Channels Found: $TOTAL_CHANNELS"
          echo "✅ Playlist Updated at: $TIME_IST"

          # Export values for commit message
          echo "count=$TOTAL_CHANNELS" >> $GITHUB_OUTPUT
          echo "time=$TIME_IST" >> $GITHUB_OUTPUT

      - name: Show output size
        run: ls -lh Shivansh.m3u || true

      - name: Commit and push changes
        shell: bash
        env:
          GIT_AUTHOR_NAME: "GitHub Actions Bot"
          GIT_AUHOR_EMAIL: "actions@github.com"
        run: |
          git config user.name "$GIT_AUTHOR_NAME"
          git config user.email "$GIT_AUTHOR_EMAIL"

          git add Shivansh.m3u
          if ! git diff --quiet --cached; then
            git commit -m "Auto-update $P Exclusive (Shivansh) [${{ steps.combine.outputs.time }}] | Channels: ${{ steps.combine.outputs.count }}"
            git pull --rebase origin main || true
            git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main
          else
            echo "No changes to Shivansh.m3u"
          fi
